# Application
NODE_ENV=development
PORT=4004

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=pti_notification
DB_LOGGING=false

# Kafka
KAFKA_BROKERS=localhost:9092

# Twilio (SMS)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=+33xxxxxxxxx

# SMTP (Email)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@pti-calendar.fr

# Web Push (VAPID)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:contact@pti-calendar.fr

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
